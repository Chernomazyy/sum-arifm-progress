using System;

namespace project3
{
    class Program
    {
        static void Main(string[] args)
        {
            int N = Convert.ToInt32(Console.ReadLine());
            int b = 0;
            for(int i = 1;i <= N;i++)
            {  b += i; }
            Console.WriteLine(b);
        }
    }
}
